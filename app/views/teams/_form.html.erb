<%= form_with(model: team, local: true, class: "space-y-4") do |form| %>
  <% if team.errors.any? %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4">
      <strong class="font-bold"><%= pluralize(team.errors.count, "error") %> prevented saving:</strong>
      <ul class="list-disc list-inside">
        <% team.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class: "block mb-1 font-semibold" %>
    <%= form.text_field :name, class: "w-full border border-gray-300 rounded px-3 py-2" %>
  </div>

  <div>
    <%= form.label :about, class: "block mb-1 font-semibold" %>
    <%= form.text_area :about, rows: 6, class: "w-full border border-gray-300 rounded px-3 py-2" %>
  </div>

  <div class="flex space-x-2">
    <%= form.submit class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded" %>
    <%= link_to "Cancel", teams_path, class: "bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded" %>
  </div>
<% end %>